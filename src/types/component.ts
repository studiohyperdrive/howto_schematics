export enum ComponentTypes {
    atom = 'atom',
    molecule = 'molecule',
    organism = 'organism',
    page = 'page',
}

export enum ComponentPrefixes {
    atom = 'a',
    molecule = 'm',
    organism = 'o',
    page = 'p',
    styleguide = 'sg',
}